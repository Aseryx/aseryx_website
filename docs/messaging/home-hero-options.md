# Home hero options (database → dataset → verify)

**Context.** How now carries the path: live database → chosen dataset → locked dataset → utility check → verified set still on your side. Hero should not restate that syllabus. Hero names the job and the terms of control. CTA stays **Join the beta**. Say **database** / **dataset**. No warehouse. No em dashes. No marketplace lead.

**Walk-away (updated).** You cut a useful dataset from the live database you already run. Aseryx checks whether that dataset is useful. The database stays where it is. Access, when you open it, is for a time you set.

**What hero is allowed to do.** Name the object (verified dataset from a live database). Name control (database stays put / you set the term). One door. Stop.

**What hero should refuse.** Four-step path. Appraisal theater. Broker / find-buyers. “Unused data” manifesto. Synonym carousel.

---

## A · Job line (**shipped** · A3)

Lead with the sentence that is the product. **Dataset** = object you cut. **Asset** = walk-away after the check.

- **H1:** Turn your live database into a useful **dataset** ↔ **asset** (live crossfade; reduced motion = dataset).
- **Body:** Scan your database for tables and fields that belong together. Lock that as a dataset and check if it is useful. When it passes, it is a data asset still on your side.
- **Micro:** The database stays where it is.
- **Walk-away.** Live database → useful dataset → data asset still on your side.
- **Why.** PG clear. Ogilvy promise is the H1. How owns the steps. Swap makes both walk-away nouns visible without a second sentence.
- **Risk.** Synonym-carousel risk; body still teaches order (dataset first, asset after the check).

---

## B · Job + control in the H1

Same job, custody baked into the headline.

- **H1:** A useful dataset from your live database. Still on your side.
- **Body:** Pick tables, joins, and fields from production. Lock that dataset. Run a quality check. You get a real dataset plus a clear answer about quality. The database does not move to us.
- **Micro:** Access, when you open it, ends on your clock.
- **Why.** Jobs: object + stance in one breath. Matches the denser custody line without saying “custody.”
- **Risk.** Two beats in H1; keep punctuation tight or it reads like a tagline stack.

---

## C · Desire first

What they walk away wanting, then how.

- **H1:** Get a dataset worth using from the database you already run.
- **Body:** Aseryx helps you choose a coherent piece of production data, lock it, and check whether it is complete, varied, and structured enough to matter. The database stays where it is.
- **Micro:** Proof before training. Proof before pay.
- **Why.** Hopkins-friendly desire. Utility without score-as-product.
- **Risk.** “Worth using” is soft unless body names the check.

---

## D · Control first

Lead with the wall, then the object.

- **H1:** Your database stays put. The dataset is what you cut and prove.
- **Body:** Choose a dataset from the live database. Lock it. Verify it is useful. That verified set is the product. Not the warehouse renamed. Not a file wrapped for its own sake.
- **Micro:** Join the beta to run the path.
- **Why.** Hard custody signal. Fights upload anxiety early.
- **Risk.** Can feel defensive; fails Hopkins if it teaches custody before naming the job. Prefer A/B unless fear is the main blocker.

Note: body says “warehouse” once as a refuse line. For live site, swap to: “Not the whole database renamed.”

---

## E · Short path, not How

Three beats that rhyme with How but stay shorter than the section.

- **H1:** Choose a dataset. Lock it. Know if it is useful.
- **Body:** Work starts in the live database you already run. You cut tables, joins, and fields into a dataset, then check quality before anyone trains or pays. The database stays where it is.
- **Micro:** Paid access is optional and timed.
- **Why.** Closest to current hero rhythm. Easy swap.
- **Risk.** Competes with How claim. Only ship if How claim softens or hero drops “know if useful” into body.

---

## F · Minimal (stress test)

- **H1:** Live database in. Useful dataset out.
- **Body:** Build the dataset. Lock it. Check it. Keep it on your side.
- **Micro:** Join the beta.
- **Why.** Distilled. High Taste Idiot Index discipline.
- **Risk.** Too cryptic for a stranger; needs How immediately below. Fail PG for some readers.

---

## Do not use (given How)

| Pattern | Why cut |
| --- | --- |
| Current: Choose / Lock / Open paid access as H1 | How + Term already own open/access; hero steals the syllabus and undersells “useful.” |
| “Unlock the value of your data” | Empty. No object. |
| Score / Appraisal as hero noun | Check is support; verified dataset is the object. |
| Marketplace / find buyers | Violates axioms; wrong door. |

---

## Pick order

1. **A3 (shipped)** — H1 = useful dataset; body earns **data asset** as walk-away.
2. **B** if custody fear is the first objection on the page.
3. **C** if the sell needs desire language before mechanism.
4. **E** only as a soft migration from the old three-beat H1.

**CTA / micro constant across options unless noted:** Join the beta · The database stays where it is.

---

## One-line recommendation

**Shipped A3.** How says database → dataset → verify. Hero says the job once; body says scan, lock, check, stays put; **asset** is what they walk away with.
